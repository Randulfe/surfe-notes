npm run format 
npm run typecheck || (echo "❌ TypeScript check failed" && exit 1)
npm run lint:fix || (echo "❌ ESLint check failed" && exit 1)
npm run format || (echo "❌ Prettier check failed" && exit 1)
echo "🎉 All checks passed!"
